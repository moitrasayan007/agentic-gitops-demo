apiVersion: v1
kind: Service
metadata:
  name: {{ include "parcel-tracker.fullname" . }}
  labels:
    {{- include "parcel-tracker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "parcel-tracker.selectorLabels" . | nindent 4 }}
